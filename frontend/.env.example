# Базовый URL бэкенда. Пустая строка = относительный путь (тот же origin,
# что и у страницы) — так работает и через nginx в проде, и локально через
# vite dev-прокси (см. vite.config.js). Указывайте абсолютный URL только если
# бэкенд реально живёт на другом origin без прокси.
VITE_API_URL=
